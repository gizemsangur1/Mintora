"use client";

import { Container, Typography } from "@mui/material";

export default function DaoPage() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        DAO Governance
      </Typography>
      <Typography>Burada proposal listesi & voting UI olacak.</Typography>
    </Container>
  );
}
